from distributions import normal

real_alpha = 1

real_beta0 = 1
real_beta1 = 2.5
noise = normal(0,1, 100)
X1 = normal(0,1,100)
X2 = normal(0,1,100)
sigma = 1

Y = real_alpha + real_beta0*X1 + real_beta1*X2 + noise*sigma

