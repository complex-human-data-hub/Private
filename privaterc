muplot = distplot(mu)

l = [e.Weather == "rain" for e in DemoEvents if e.hasField("Weather")]

users = set([e.UserId for e in DemoEvents])

userindex = dict([[u,i] for i,u in enumerate(users)])

userids = [userindex[e.UserId] for e in DemoEvents if e.hasField("latitude")]


lats = [e.latitude for e in DemoEvents if e.hasField("latitude")]

lats ~ Normal(mu, sigma)

mu ~ Normal(0,100)
sigma ~ HalfNormal(100)

